<p><%= (I18n.t 'hello').capitalize %> <%= @resource.email %>!</p>

<p><%= I18n.t 'email.account_locked.intro' %></p>

<p><%= link_to I18n.t('email.account_locked.button'), unlock_url(@resource, unlock_token: @token) %></p>

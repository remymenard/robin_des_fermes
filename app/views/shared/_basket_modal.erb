<div id="basket-modal-content">
  <div id="close-basket-modal"><i class="fas fa-times"></i></div>
  <% product = Product.find(@last_added.product_id) %>
  <% farm = Farm.find(product.farm_id) %>
  <%= farm.name %>
</div>
<% unless flash.empty? %>
    <% flash.each do |key, value| %>
      <% type = key.to_s.gsub('alert', 'error').gsub('notice', 'success') %>
      <span class="railsAlert" data-type="<%= type %>" data-text="<%= value %>"></span>
    <% end %>
<% end %>
